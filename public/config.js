// config.js  — change BACKEND_URL after deploying backend to Vercel
window.QLCE_CONFIG = {
  BACKEND_URL: "http://localhost:4000",
  // After deploying backend, change to:
  // BACKEND_URL: "https://qlce-backend.vercel.app",
};
